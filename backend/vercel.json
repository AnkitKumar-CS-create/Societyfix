{
  "version": 2,
  "buildCommand": "npx prisma generate",
  "builds": [
    {
      "src": "api/[...route].ts",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/api/[...route].ts"
    }
  ]
}
